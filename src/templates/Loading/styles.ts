import styled from 'styled-components';

export const Wrapper = styled.div`
  margin-top: 30%;
  transform: translateY(-50%);

  svg {
    display: block;
    margin: 0 auto;
    max-width: 30%;
  }
`;
