export type ContactFormData = {
  id: string;
  type: string;
  description: string;
};

export type Contact = {
  id: string;
  type: string;
  description: string;
  created_at: string;
  updated_at: string;
};
